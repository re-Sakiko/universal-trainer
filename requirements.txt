torch>=2.5.0
transformers>=4.45.0
datasets>=3.0.0
peft>=0.12.0
trl>=0.12.0
accelerate>=1.0.0
# 可选: CUDA 加速 (NVIDIA GPU)
# bitsandbytes>=0.45.0
# 可选: AMD ROCm 加速 (Linux)
# PyTorch ROCm 构建: pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
# bitsandbytes (ROCm): pip install bitsandbytes>=0.45.0
# Flash Attention (ROCm): pip install flash-attn --no-build-isolation
# 可选: AMD/Intel GPU (Windows)
# torch-directml>=0.2.5
